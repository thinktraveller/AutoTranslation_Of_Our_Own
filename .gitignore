# 环境变量与密钥（绝对不能提交）
.env
*.env
.env.*
!.env.example

# Python 虚拟环境
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 验证脚本（临时，验证后删除）
_verify/

# 构建临时文件与运行时产物
_ato3_progress.json
.translation_checkpoint.json
llm_calls.jsonl

# 任务日志（运行时生成，不纳入版本控制；仅保留目录本身）
logs/*
!logs/.gitkeep

# 本地配置（含 API 密钥，不上传）
config.json

# IP 词典（版权内容，不上传）
dicts/ip/
dicts/dict_index.json

# 本地文档（不上传）
docs/

# Claude Code 本地数据
.claude/

# IDE
.idea/
.vscode/
*.swp
*.swo

# 输出文件（可选：若不希望将译文纳入版本控制）
# *_translated.txt
# *_translated.md
# *_translated.docx
